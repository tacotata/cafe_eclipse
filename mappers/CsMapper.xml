<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.mapper.CsMapper">

	<insert id = "addCs">
			INSERT INTO cs (cs_id, cs_email, cs_title, cs_content, cs_img, reg_date)
			VALUES (#{csId}, #{csEmail}, #{csTitle}, #{csContent}, #{csImg}, #{regDate})
	</insert>
</mapper>